spring.application.name=healthupdate

spring.datasource.url=jdbc:mysql://localhost:52577/healthdb
spring.datasource.username=healthuser
spring.datasource.password=saunil900
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Shows the SQL queries generated by Hibernate in the console. Useful for debugging.
spring.jpa.show-sql=true

# Formats the SQL shown in the console to be more readable.
spring.jpa.properties.hibernate.format_sql=true

gemini.api.key=your api key
